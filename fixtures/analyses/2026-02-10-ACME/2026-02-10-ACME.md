# ACME - synthetic fixture (2026-02-10-ACME)

**This is not an analysis.** Every figure in the accompanying spec is
invented, and ACME is not a real listing. The file exists so the
public test suite has a stable corpus to assert over without depending
on the private research record.

Shape this fixture carries: a second run of the same ticker, so the timeline view has a series to draw

See `tests/conftest.py`.
