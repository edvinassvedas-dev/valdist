# FLOORCO - synthetic fixture (2026-04-20-FLOORCO)

**This is not an analysis.** Every figure in the accompanying spec is
invented, and FLOORCO is not a real listing. The file exists so the
public test suite has a stable corpus to assert over without depending
on the private research record.

Shape this fixture carries: floored draws - cost_of_equity and affo_terminal bands cross, so the portfolio's floored badge has real data behind it

See `tests/conftest.py`.
