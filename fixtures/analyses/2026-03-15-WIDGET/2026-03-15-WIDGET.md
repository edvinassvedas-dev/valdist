# WIDGET - synthetic fixture (2026-03-15-WIDGET)

**This is not an analysis.** Every figure in the accompanying spec is
invented, and WIDGET is not a real listing. The file exists so the
public test suite has a stable corpus to assert over without depending
on the private research record.

Shape this fixture carries: the equity_v2 adapter, so the suite does not assert only over REITs

See `tests/conftest.py`.
