# PREFCO - synthetic fixture (2026-05-25-PREFCO)

**This is not an analysis.** Every figure in the accompanying spec is
invented, and PREFCO is not a real listing. The file exists so the
public test suite has a stable corpus to assert over without depending
on the private research record.

Shape this fixture carries: the preferred_v1 adapter, whose spec shape has two drivers rather than six

See `tests/conftest.py`.
